resources:
  - name: scriptGit
    type: GitRepo
    configuration:
      path: rahulnlb1/Org_test_Automation
      gitProvider: s_gitHub
      branches:
        include: ^{{gitBranch}}$

pipelines:
  - name: pipeline_1
    configuration:
      runtime:
        type: image
        image:
          auto:
            language: node
            versions:
              - "10.18.0"
    steps:
       - name: S_Bash_0577
         type: Bash
         configuration:
           inputResources:
              - name: scriptGit
         execution:
           onExecute:
             - echo $res_scriptGit_resourcePath
             - pushd $res_scriptGit_resourcePath/tests/core/oom
             - node oom.js
             - popd